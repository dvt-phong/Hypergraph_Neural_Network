[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mooc-dropout-hypergraph"
version = "0.1.0"
description = "MOOC dropout prediction and hypergraph baseline reproduction"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["src", "models"]
